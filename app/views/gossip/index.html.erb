<header class="flex-fill">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/home">Home</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link" href="/gossip">Index des potins <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" href="/gossip/new"> Creation de potin <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" href="/team">Team <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" href="/contact">Contact</a>
        </li>
      </ul>
    </div>
  </nav>
</header>

<body class="bg-secondary text-light d-flex align-items-stretch flex-wrap">
  <section class="container-fluid">
    <div class= "text-light  d-flex align-content-end flex-wrap ">
      <p class="p-6"><br/>Salut ! Ici c'est notre super index des potins, il est chouette, non ?<br/></p>
    </div>
    <div class="container-fluid d-flex align-content-between justify-content-between flex-wrap">
      <% @gossip.each do |gossip| %>
        <div class = "card" style = "width: 25rem;">
          <div class="bg-light text-dark">
            <div class = "card-body">
              <h6 class = "text-success">Gossip n°: <%= gossip.id%></h6>
              <p class = "card-text">
                L'auteur: <%= gossip.user.first_name %> <%= gossip.user.last_name %><br/>
                Le titre : <%= gossip.title%><br/>
                Le contenu :<%= gossip.content %>
              </p>
              <p class= "flex-wrap">
                <a href="/gossip/<%= gossip.id %>" class="card-link">Lien vers le potin ici</a> 
                <a href="/gossip/<%= gossip.id%>/edit" class="card-link">Editer le potin ici</a>           
              </p>
            </div>
          </div>
        </div>
        <br/>
      <% end %> 
    </div>
  </section>
</body>